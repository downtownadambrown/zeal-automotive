import { FC } from "react"

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div className="pt-36 flex flex-col h-48 text-black">
        Services
    </div>
  )
}

export default page